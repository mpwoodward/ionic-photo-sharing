import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mattwoodward.photosharingapp',
  appName: 'ionic-photo-sharing',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
